# Zendesk Support SDK Credentials
# Get these from: https://rabblelabs.zendesk.com/admin → Apps and integrations → APIs → OAuth Clients
#
# When you create an OAuth client, Zendesk shows:
# - "Unique identifier" → Copy to ZENDESK_APP_ID (this is the client ID)
# - "Secret" → Copy to ZENDESK_CLIENT_ID (this is the client secret - yes, confusingly named!)
#
ZENDESK_APP_ID=         # Zendesk OAuth "Unique identifier"
ZENDESK_CLIENT_ID=      # Zendesk OAuth "Secret"
ZENDESK_URL=https://rabblelabs.zendesk.com
